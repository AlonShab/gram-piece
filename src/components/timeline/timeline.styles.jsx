import { css } from "styled-components";

export const timeline = css(({ theme }) => {
  return css`
    border: 1px solid grey;
    padding: 10rem;
    text-align: center;
    font-size: 2rem;
  `;
});
